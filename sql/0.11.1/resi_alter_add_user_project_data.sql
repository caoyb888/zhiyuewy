-- ------------------------------------------------------------
-- S1-07 项目权限隔离：初始化用户项目关联数据
-- 说明：
--   1. 超管（user_id = 1）无需在此表配置，代码中自动视为全部权限
--   2. 其他用户需在管理后台为其分配项目权限后，方可查看对应项目数据
--   3. 若用户在此表无记录，则默认无任何项目权限（查询返回空结果）
-- ------------------------------------------------------------

-- 示例：为测试账号分配项目权限（如有测试项目，请按实际 project_id 调整）
-- INSERT INTO `resi_user_project` (`user_id`, `project_id`, `create_by`, `create_time`)
-- VALUES (2, 1, 'admin', NOW());
